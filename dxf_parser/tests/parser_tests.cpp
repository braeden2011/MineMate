#include <catch2/catch_test_macros.hpp>

// Placeholder test — always passes.
// Real DXF parser tests are added in Phase 1.
TEST_CASE("Placeholder test passes", "[dxf_parser]")
{
    REQUIRE(1 + 1 == 2);
}
